# 🧠 DRGR Psycho-Platform

Модуль психодиагностики и мягкой психокоррекции на основе анализа лицевой экспрессии (FER), оценки возраста и текстовых тестов.

## Концепция

Веб-платформа, где пользователь проходит тесты (опросники, устные задания на видео), а система по лицевой экспрессии и голосу оценивает психоэмоциональное состояние и предлагает рекомендации/упражнения.

**Основа:**
- Современные работы по автоматическому распознаванию эмоций (FER) 2021–2026
- Популярная невербика (Пиз — чтение по позам/микромимике, без претензии на научную строгость)
- Общие труды по психокоррекции и психопрофилактике

**Ограничения:**
- ⚠️ Система **не ставит диагнозы** — только мягкая оценка и рекомендации
- ⚠️ Платформа **21+** — с проверкой возраста по лицу (вероятностная, не юридическая)
- ⚠️ FER-модели уступают человеку и требуют валидации перед клиническим использованием

## Структура модуля

```
psycho_platform/
├── __init__.py         # Инициализация модуля
├── schema.sql          # Схема БД (SQLite / PostgreSQL)
├── fer_config.py       # Конфигурация FER-pipeline и age-estimation
├── age_gate.py         # Модуль проверки возраста 21+
├── consent.py          # Информированное согласие и приватность
└── README.md           # Этот файл
```

## База данных

Схема (`schema.sql`) включает таблицы:

| Таблица | Назначение |
|---------|-----------|
| `sources` | Научные источники (статьи, книги, методички) |
| `methods` | Методики (дыхательные, когнитивные, телесные) |
| `users` | Пользователи (минимум данных, UUID, без ФИО) |
| `test_sessions` | Сессии тестирования |
| `fer_results` | Результаты FER-анализа (агрегированные метрики, НЕ видео) |
| `recommendations` | Выданные рекомендации |

## Архив для обучения ВМ

PowerShell-скрипт `setup_training_archive.ps1` (в корне репозитория) создаёт структуру каталогов и скачивает открытые ресурсы:

```powershell
# Базовый запуск
.\setup_training_archive.ps1

# Свой путь
.\setup_training_archive.ps1 -RootDir D:\ML_Archive

# Дополнительные ссылки
.\setup_training_archive.ps1 -ExtraUrls @("https://example.com/data.zip")

# Из CSV-файла
.\setup_training_archive.ps1 -UrlFile my_urls.csv
```

Структура архива:
```
C:\VM_training_archive\
├── datasets\           FER, age, nonverbal, custom датасеты
├── papers\             Научные статьи и обзоры
├── methods_puzenko\    Материалы Пузенко В.Ю.
├── site_backup\        Бэкап сайта
├── models\             Предобученные модели
└── logs\               Журнал скачиваний
```

## Модуль видеоанализа (FER Pipeline)

### Архитектура

```
Browser (WebRTC) → 5-10 fps кадров → Backend (Python + PyTorch)
                                        ├── Детекция лица (RetinaFace/MTCNN)
                                        ├── FER-модель (ViT/ResNet)
                                        ├── Временное сглаживание
                                        └── Агрегация метрик
```

### Выход модели

Для каждого временного интервала (привязан к вопросу теста):
- **Валентность** (−1..+1): негатив → позитив
- **Возбуждение** (0..1): спокойствие → возбуждение
- **Уровень стресса** (0..1): низкий → высокий
- **Доминирующая эмоция**: joy / sadness / anger / fear / surprise / neutral
- **Уверенность** модели (0..1)

### Интерпретация

Вместо диагнозов — мягкие формулировки:
> «Во время ответа на сложные вопросы у вас заметно возрастает выражение эмоций, связанных со стрессом.»
> «Текущий уровень эмоционального напряжения: умеренный.»

Далее — связка с базой методик → рекомендации (дыхательные, телесные, когнитивные упражнения).

## Проверка возраста (21+)

1. При входе показываем камеру + текст согласия
2. Модель age estimation → оценка + доверительный интервал
3. Если `нижняя_граница ≥ 21` → доступ
4. Иначе → отказ с сообщением

## Этика и приватность

- ✅ Обязательное информированное согласие **до** записи камеры
- ✅ Добровольность: без камеры — только текстовые тесты
- ✅ По умолчанию хранятся только анонимизированные метрики (не видео)
- ✅ Никаких «диагнозов» — только рекомендации
- ✅ Для несовершеннолетних — отказ с направлением к специалисту
